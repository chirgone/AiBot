{
  "name": "agentica",
  "version": "1.3.0",
  "description": "AngaFlow conversational studio and multi-tenant voice runtime on Cloudflare Workers, D1, Durable Objects, Workers AI, and Twilio.",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "types": "wrangler types",
    "typecheck": "tsc --noEmit",
    "check": "wrangler deploy --dry-run && tsc --noEmit"
  },
  "keywords": [
    "cloudflare-workers",
    "durable-objects",
    "twilio",
    "workers-ai"
  ],
  "author": "",
  "license": "ISC",
  "type": "module",
  "devDependencies": {
    "@types/node": "^26.1.2",
    "typescript": "^7.0.2",
    "wrangler": "^4.118.0"
  }
}
